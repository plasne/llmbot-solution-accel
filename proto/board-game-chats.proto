syntax = "proto3";

option csharp_namespace = "BoardGameChat";
package BoardGameChat;

service BoardGameChats {
  rpc Chat (ChatRequest) returns (stream ChatResponse);
}

message ChatRequest {
  string usr = 1;
}

message ChatResponse {
  string msg = 1;
}